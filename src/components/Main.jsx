import { View } from 'react-native';
import RepositoryList from './RepositoryList';

export default function Main() {
  return (
    <View style={{ flex: 1 }}>
      <RepositoryList />
    </View>
  );
}
